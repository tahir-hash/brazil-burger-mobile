import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'brazil-burger-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
