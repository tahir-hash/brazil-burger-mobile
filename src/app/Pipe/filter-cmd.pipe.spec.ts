import { FilterCmdPipe } from './filter-cmd.pipe';

describe('FilterCmdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCmdPipe();
    expect(pipe).toBeTruthy();
  });
});
