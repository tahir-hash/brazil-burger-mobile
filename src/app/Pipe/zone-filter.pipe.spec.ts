import { ZoneFilterPipe } from './zone-filter.pipe';

describe('ZoneFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ZoneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
